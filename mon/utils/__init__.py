"""Reserved for small, cross-cutting helpers that don't belong to a single
layer (engine/analyzers/parsers/network/exporters/models). Currently empty --
add functions here only once something genuinely needs to be shared across
multiple of those packages; until then, keep logic in its owning layer.
"""
